CREATE TABLE products
(
  id          INTEGER NOT NULL,
  price       INTEGER NOT NULL,
  name        VARCHAR(20),
  company     VARCHAR(20) NOT NULL,
  quantity    INTEGER NOT NULL,
  date        DATE NOT NULL
);
